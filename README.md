## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. (Optional) Set the `API_KEY` in `.env.local` for any additional services you may use.
3. Run the app:
   `npm run dev`
